# 4M0906014__0005

**ECU:** Bosch MDG1
**Vehicle:** VAG platform (Porsche/Bentley/Lamborghini 4.0T shared ECU)
**Endianness:** Little-endian

---

## Write Parameters

| Parameter | Value |
|-----------|-------|
| mid_byte | `0x80` |
| address_offset | `0x000000` (direct) |
| Max chunk size | 64 bytes |

Write frame: `3E 39 01 80 [addr_2] [addr_1] [addr_0] [data...]`

## Special Addresses

| Purpose | Address | Description |
|---------|---------|-------------|
| Ethanol memory | `0x11E46A` | Write current ethanol % for flex fuel blending |
| Speed display | `0x09F90E` | Speed limiter / display override |

---

## Logger Variables (4)

4 required, 0 optional. This is the **minimal** variable set — only required variables are defined.

| Variable | Display Name | Address | Size | Scale | Offset | Unit |
|----------|-------------|---------|------|-------|--------|------|
| `nmot_w` | Engine Speed | `0x60020514` | 2B | 0.25 | 0 | rpm |
| `InjSys_ratEthPrtnBascFu` | Ethanol Content | `0x60015162` | 2B | 1.526e-3 | 0 | % |
| `rlp_w` | Load (predicted) | `0x5001CB32` | 2B | 2.344e-2 | 0 | % |
| `Com_stCrCtlPan` | Cruise Control Status | `0x600205F4` | 2B | 1.0 | 0 | — |

### Value Conversion

```
physical = (raw_uint16 × scale) + offset
```

### Notes

- Only 4 variables — no coolant temp, throttle, boost, timing, or fuel trims
- All addresses are slightly offset from the `4K0907557G` and `4M0906014B` variants (different firmware build)
- Same mid_byte (`0x80`) and direct addressing as other non-8W boxcodes
