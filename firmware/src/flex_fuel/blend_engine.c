#include "blend_engine.h"
#include "esp_log.h"

static const char *TAG = "blend_engine";

esp_err_t blend_engine_init(void)
{
    ESP_LOGI(TAG, "Blend engine initialized (stub)");
    return ESP_OK;
}
